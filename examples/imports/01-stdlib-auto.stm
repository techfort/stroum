-- Example 1: Using stdlib functions without explicit import
-- The stdlib is automatically imported, so all functions are available

f:main -> Void =>
  add(5, 3)
  |> mul(2, _)
  |> println()

main()
