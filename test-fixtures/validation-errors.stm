-- Test file with validation errors

-- Duplicate function names
f:process data => data |> transform
f:process other => other |> validate

-- Function with rec but no self-reference
rec f:double n => multiply(n, 2)

-- Multiple outcome paths without emission contract
f:fetch url =>
  http_get(url) @"ok"
  | .error => @"fail"
