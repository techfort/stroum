-- Stroum Standard Library - Simple Test
-- Tests basic arithmetic and I/O functions

f:double x => mul(x, 2)

f:add_ten x => add(x, 10)

f:compute => double(5) |> add_ten() |> debug("Result")

compute()
